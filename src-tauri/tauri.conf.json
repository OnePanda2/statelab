{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "StateLab",
  "version": "0.1.0",
  "identifier": "local.statelab.app",
  "build": {
    "frontendDist": "../dist",
    "devUrl": "http://localhost:1420",
    "beforeDevCommand": "npm run dev",
    "beforeBuildCommand": "npm run build"
  },
  "app": {
    "windows": [
      {
        "title": "StateLab",
        "width": 1360,
        "height": 900,
        "minWidth": 900,
        "minHeight": 600,
        "resizable": true
      }
    ],
    "security": {
      "csp": "default-src 'self'; img-src 'self' data: blob:; style-src 'self' 'unsafe-inline'; connect-src 'self' ipc: http://ipc.localhost"
    }
  },
  "bundle": {
    "active": true,
    "targets": ["msi", "nsis"],
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "shortDescription": "Deterministic state-evolution research platform",
    "longDescription": "StateLab is a deterministic state-evolution research platform. Classic Collatz is its first built-in system, not its purpose: the engine is generic and computes every trajectory in arbitrary precision."
  }
}
